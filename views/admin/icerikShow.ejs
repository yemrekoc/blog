<% include ./adminPartials/header %>
<div class="content-wrapper">
    <div class="container-fluid">
             <!-- Breadcrumbs-->
         <ol class="breadcrumb">
                <li class="breadcrumb-item">
                  <a href="/admin">Ana Sayfa</a>
                </li>
                <li class="breadcrumb-item active">İçerikler</li>
              </ol>
              <!-- Example DataTables Card-->
              <div class="card mb-3">
                <div class="card-header">
                  <i class="fa fa-table"></i>İçerik Tablosu</div>
                <div class="card-body">
                  <div class="table-responsive">
                    <table class="table table-bordered table-striped" id="dataTable" width="100%" cellspacing="0">
                      <thead>
                        <tr>
                          <th>İçerik Menü</th>
                          <th>İçerik Başlık</th>
                          <th>Düzenle</th>
                          <th>Sil</th>
                        </tr>
                      </thead>
                      <tbody>
                        <% blnIcerik.forEach(function(icerik){ %>
                            <tr>
                                <td><%= icerik.menu.menuName %></td>
                                <td><%= icerik.icerikTitle %></td>
                                <td><a href="/admin/icerik-duzenle/<%=icerik._id%>">Düzenle</a></td>
                                <td>
                                  	<form action="/admin/icerikler/<%=icerik._id%>?_method=DELETE" method="POST">
                                      <button class="btn btn-danger">Sil</button>
                                    </form>
                                </td>
                            </tr>
                        <% });%>
                       </tbody>
                    </table>
                   </div>
                </div>
            </div>      
        </div>   
    </div>
  </div>
  <% include ./adminPartials/footer %>